# package-manager

Расширение 1С «Менеджер пакетов»: реестр источников, каталог пакетов,
установка `.cfe`, `.epf` и `.erf` из реестра пакетов провайдера прямо
из информационной базы.

## Как это работает

Источник заводится **один раз**: провайдер, адрес сервера, путь проекта и,
для приватного проекта, токен. Дальше пакеты и версии подтягиваются сами из
манифеста `packages.json` в проекте — прописывать ссылку на каждый пакет
вручную не нужно.

```text
Источник (строка справочника)
  └── packages.json          манифест каталога в репозитории проекта
        ├── categories[]     разделы для интерфейса
        └── packages[]       имя, описание, категория
              └── версии     из реестра пакетов того же проекта
                    └── файлы (.cfe / .epf / .erf)
```

Если манифеста нет, источник работает в режиме одного пакета.

## Возможности

- Каталог пакетов с категориями и списком версий.
- Выбор версии по semver, предвыбор новейшей, фильтр по типу релиза.
- Установка расширений `.cfe` с проверкой применимости.
- Открытие внешних обработок и отчетов `.epf` / `.erf` без сохранения на диск.
- История установок с источником, версией и файлом.
- Проверка обновлений вручную или регламентным заданием.
- Генератор шаблонов CI/CD для публикации пакетов.

Провайдеры: **GitLab** реализован, GitHub и Gitea — по готовому контракту.

## Требования

Платформа **8.3.23** и выше. Режим совместимости расширения — `8.3.23`,
поэтому расширяемая конфигурация должна работать в режиме совместимости
не ниже. Ограничение подробно описано в [doc/ARCHITECTURE.md](doc/ARCHITECTURE.md).

## Тесты

Модульные тесты — YAxUnit, сценарные — Vanessa Automation.
Подготовка окружения и запуск: [tests/README.md](tests/README.md).

## Документация

| Документ | О чем |
| -------- | ----- |
| [doc/ARCHITECTURE.md](doc/ARCHITECTURE.md) | Архитектурные решения |
| [doc/PLAN.md](doc/PLAN.md) | Этапы работ и отступления от плана |
| [doc/TOKENS.md](doc/TOKENS.md) | Токены доступа и права |
| [CLAUDE.md](CLAUDE.md) | Правила работы с репозиторием |
| [tests/README.md](tests/README.md) | Тесты: подготовка и запуск |

## Лицензии заимствованного кода

| Компонент | Лицензия |
| --------- | -------- |
| Коннектор (`пм_КоннекторHTTP`) | Apache-2.0 |
| Модули БСП | CC BY 4.0 |

Каталог `build/YellowPM` — референс интерфейса под лицензией GPL-3.0,
код из него не заимствуется.
