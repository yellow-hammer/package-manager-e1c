// Модуль управляемого приложения тестовой конфигурации-заглушки.
//
// Собственной логики у конфигурации нет: она нужна только как основа,
// к которой применяются расширения проекта. Обработчик ПриНачалеРаботыСистемы
// объявлен намеренно пустым - к нему подключается движок YAxUnit через
// &После("ПриНачалеРаботыСистемы"). Без этого обработчика расширение движка
// не получает управление, и модульные тесты молча не запускаются.

Процедура ПриНачалеРаботыСистемы()

	// Намеренно пусто, см. комментарий к модулю.

КонецПроцедуры
