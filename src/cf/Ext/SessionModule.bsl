// Модуль сеанса тестовой конфигурации-заглушки.
//
// Обработчик объявлен намеренно пустым: к нему подключается расширение
// package-manager через &После("УстановкаПараметровСеанса"), чтобы задать
// параметр сеанса пм_ПараметрыКлиентаНаСервере. Без этого обработчика
// расширение не получает управление, параметр сеанса остается неустановленным,
// и сеанс падает с невосстановимой ошибкой при первом обращении к нему.

Процедура УстановкаПараметровСеанса(ИменаПараметровСеанса)

	// Намеренно пусто, см. комментарий к модулю.

КонецПроцедуры
